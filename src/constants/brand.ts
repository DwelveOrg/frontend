/** Canonical product name — import this everywhere instead of using the raw string. */
export const BRAND_NAME = "Dwelve" as const;

/** Tailwind classes applied to the wordmark text inside DwelveLogo — keeps font style consistent across all contexts. */
export const BRAND_WORDMARK_CLASSES = "font-bold tracking-tight leading-none" as const;
