# Moved: Frontend Architecture

The canonical frontend architecture rules now live at:

```txt
docs/architecture/ARCHITECTURE.md
```

Read that file for backend request, schema, form, server action, and React Query
rules. This file remains only so older references do not break.
